# === DADOS / BACKUPS — NUNCA versionar (vazamento) ===
# O portal versiona só CÓDIGO. Os backups de dados ficam FORA do git — no
# Drive/local do dono. Ver docs/backup.md.
*.csv
backup_*/
*.dump
*.sql.gz
divat_security_snapshot_*.sql

# === Segredos ===
.env
.env.*

# === Node (só os testes usam Node; não há build nem dependências) ===
node_modules/
npm-debug.log*

# === Semgrep (análise estática; binário instalado à parte, ver docs/semgrep.md) ===
# As REGRAS (.semgrep/) são versionadas; o binário e o cache, não.
.venv-semgrep/
.semgrep_logs/
.semgrep/cache/

# === Lixo de SO / editor ===
.DS_Store
Thumbs.db
*.swp
*~
.vscode/
.idea/

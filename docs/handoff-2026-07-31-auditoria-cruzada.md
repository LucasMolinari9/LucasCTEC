# Handoff — auditoria cruzada de 31/07/2026

Ponto de partida para a próxima sessão. **Só o que falta e o que só o dono pode fazer.**
O que foi feito e por quê está no `docs/CHANGELOG.md` e nas mensagens de commit — não repito aqui.

## Estado

- **PR #73 mergeado** (`0bfb38a`). A `main` tem `supabase/migrations/`, 8 workflows, e a Fase 3
  continua aplicada **só no teste**.
- **Branch `claude/gpt-sol-report-9jrx5h`**, 6 commits à frente da `main`, empurrada, **sem PR
  aberto**. É onde estão os achados 1, 2, 3, 4 (parte de código) e 5-9 da auditoria.
- Tudo que roda offline está verde: `tests/check.js`, as 4 bancadas, `check_migrations.mjs`.

## O que só você pode fazer

1. **Abrir o PR da branch e mergear.** Não abri — o padrão é não abrir PR sem você pedir.
   No PR, o `deriva.yml` e o `db-checks.yml` rodam contra o banco e verificam o que eu não pude
   (ver "Não verificado" abaixo).
2. **Leaked Password Protection** — Dashboard → Authentication. Único item de segurança do
   relatório ainda aberto. É um toggle.
3. **As 3 pendências operacionais do #73:** criar/rotacionar `divat_auditor_ci` com
   `scripts/bootstrap_phase3_auditor.sql`; gravar o secret `SUPABASE_TEST_AUDIT_DATABASE_URL`;
   configurar branch protection.
   > Ao configurar branch protection, marque como obrigatórios **só** `check`, `views` e
   > `semgrep`. Os checks com filtro de `paths` ficam pendentes para sempre em PRs que não os
   > acionam, e travam o merge.
4. **Exercício de restore + RTO/RPO** (`docs/backup.md`, passo 9, ainda `(não medido)`).
   Precisa de um projeto Supabase descartável e da service key. **É o item mais antigo em aberto
   do projeto** e o único que fecha o SEC-06.
5. **Trocar as chaves para `sb_publishable_…`** quando quiser. O código já aceita os dois
   formatos: é editar `SB_KEY` e `SB_TESTE_KEY` no topo do `app.js`. Prazo: fim de 2026.

## Não verificado — precisa de rede, e o CI é quem confirma

Nada disto roda do ambiente do Claude (sem acesso a `*.supabase.co`).

- **`check_deriva.mjs`** — mexi no `docs/schema.md`, que ele valida coluna a coluna, e no
  comentário do `NAO_TABELA`. **É a verificação mais importante pendente.**
- **`check_ambientes.mjs`** (gate novo) — nunca rodou contra os bancos reais. O baseline nasceu
  vazio com uma **previsão escrita dentro dele**: não deve haver achado, porque a Fase 3 divergiu
  o teste no que o portal não usa. Se a primeira execução falhar, a previsão estava errada e o
  achado é real — investigue antes de baselinar.
- **`restore_rest.mjs`** — testado só contra o stub da bancada, nunca contra um PostgREST real.
  Três coisas que só um Supabase de verdade resolve: se `?col=not.is.null` é aceito como filtro
  de DELETE; se lotes de 500 linhas cabem no limite de requisição nas tabelas largas
  (`itinerario_teste` tem 52k linhas de texto); se `GENERATED BY DEFAULT AS IDENTITY` aceita
  `row_id` explícito vindo por REST. **Descubra isso num projeto descartável, não num restore de
  verdade** — junte com o item 4 acima.
- `check_views.mjs`, `check_abas.mjs` e Semgrep — faltam Chromium e Semgrep aqui. O `app.js` foi
  tocado (só o `sbFetch`), então desta vez eles têm alvo no diff.

## Dívida que continua registrada, e agora com dono

- **O `check_ambientes.mjs` vê os dois bancos como `anon` vê.** Índices, policies, RLS, grants e
  triggers do projeto de **teste** seguem sem vigilância nenhuma — o `check_grants.mjs` só olha
  produção. É a próxima peça se quiser fechar o eixo.
- **17 codlinhas órfãs** em produção (`scripts/data_quality_baseline.json`). Dívida de dado, não
  de código. As 7 de `evento_teste` são arquivo institucional — **não apagar**.
